# Audio files are loaded locally for dev only — drop your tracks next to
# index.html and the page will fetch them via TRACK_AUDIO_URL.
# Don't commit copyrighted audio to a public repo.
*.mp3
*.wav
*.ogg
*.m4a
*.flac

# Beat-alignment scratch files (analysis scripts, diagnostic plots, decoded PCM)
_*.py
_*.csv
_*.json
_*.txt
_*.png
_*.f32

# Wrangler caches (created if you run wrangler from this directory)
.wrangler/
